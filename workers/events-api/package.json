{
  "name": "gsnbhs-events-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply gsnbhs-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
